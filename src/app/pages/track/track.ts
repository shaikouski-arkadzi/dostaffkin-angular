import { Component } from '@angular/core';
import { Header } from '../../header/header';

@Component({
  selector: 'app-track',
  imports: [Header],
  templateUrl: './track.html',
  styleUrl: './track.css',
})
export class Track {}
