import { Component } from '@angular/core';
import { Header } from '../../header/header';

@Component({
  selector: 'app-order',
  imports: [Header],
  templateUrl: './order.html',
  styleUrl: './order.css',
})
export class Order {}
